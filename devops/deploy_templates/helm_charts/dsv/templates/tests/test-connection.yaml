apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "generico.fullname" . }}-test-connection"
  labels:
    {{- include "generico.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "generico.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
